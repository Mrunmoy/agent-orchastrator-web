# Development Workflow Contract

For every feature/slice:
1. Write design doc first (`docs/design/`).
2. Write failing unit tests.
3. Implement in small modular commits.
4. Run integration tests and collect evidence.
5. Record findings in playbook (`docs/playbook/`).
6. Append progress to saga (`docs/saga/`).
7. Update decisions/changelog where applicable.
